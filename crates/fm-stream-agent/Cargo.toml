[package]
name = "fm-stream-agent"
description = "Skeleton of the Rust agent port. The serve command is not implemented yet; bin/fm-stream-agent.py remains the reference."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[[bin]]
name = "fm-stream-agent"
path = "src/main.rs"

[dependencies]
fm-stream-wire.workspace = true
