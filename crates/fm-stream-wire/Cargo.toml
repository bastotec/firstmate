[package]
name = "fm-stream-wire"
description = "Shared wire types, hub protocol version, and the byte-exact Bridge record encoder for the Rust stream backend port."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
